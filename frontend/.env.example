# Backend API URL (for server-side calls)
API_URL=http://skills-api:8001

# Bearer token for backend auth
BEARER_TOKEN=your_token_here

# Public env vars (embedded at build time for client-side)
PUBLIC_API_URL=http://localhost:8001
