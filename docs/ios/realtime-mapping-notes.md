# Realtime Mapping Notes (Doc Only)

## Notes
- Map `metadata.folder` to folder path.
- `metadata.pinned` and `metadata.pinned_order` should drive pinning UI.
- `deleted_at` indicates removal.

## Websites
- Map `metadata.pinned` and `metadata.pinned_order`.
- `metadata.archived` indicates archive state.
- `metadata.youtube_transcripts` should update transcript badges.

## Ingested Files
- Job updates map to processing status badges.
- If job arrives for missing file, trigger list refresh.
