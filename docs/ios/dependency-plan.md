# iOS Dependency Plan

## Required
- Supabase Swift SDK (auth + realtime)
- MarkdownUI (markdown rendering)

## Candidate / Optional
- RichTextKit (markdown editing)
- Kingfisher or SDWebImageSwiftUI (image caching)
- Sentry (telemetry, optional)

## Notes
- Favor native frameworks over third-party when possible.
- Re-evaluate RichTextKit after MVP (editing phase).
